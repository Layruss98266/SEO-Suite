"""
Misc blueprint — /, /login, /logout, /health,
                 /api/use_cases, /api/tasks.
"""

from flask import Blueprint

misc_bp = Blueprint("misc", __name__)

# TODO: migrate routes from server.py
