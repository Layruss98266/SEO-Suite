# Web framework
flask==3.1.3
flask-cors==6.0.2
flask-sqlalchemy==3.1.1
flask-session==0.7.0
flask-limiter==3.5.0
# Werkzeug ships with Flask and provides generate_password_hash / check_password_hash
# used by core/auth.py — no extra dep needed for session auth.

# Browser automation (indexing checker)
playwright==1.59.0

# HTTP & HTML parsing
requests==2.33.1
httpx[http2]==0.27.2
beautifulsoup4==4.14.3
lxml==6.0.3
html5lib==1.1
bleach==6.1.0

# Reports & data
openpyxl==3.1.5
pandas==2.2.3

# CLI & display
colorama==0.4.6
tqdm==4.67.3

# Scheduling
schedule==1.2.2
APScheduler==3.10.4

# Google APIs (PageSpeed, Search Console)
google-api-python-client==2.195.0
google-auth==2.50.0
google-auth-httplib2==0.3.1

# Config / secrets management
python-dotenv==1.2.2

# Input validation & data models
pydantic==2.6.0
pydantic-settings==2.1.0

# Monitoring & error tracking
sentry-sdk==1.40.0
python-json-logger==2.0.7

# Security
cryptography==42.0.0

# Production WSGI server
waitress==3.0.2

# ═══════════════════════════════════════════════════════════════════════════
# DEV DEPENDENCIES — install with: pip install -r requirements.txt -e '.[dev]'
# ═══════════════════════════════════════════════════════════════════════════

# Testing
pytest==7.4.4
pytest-cov==4.1.0
pytest-mock==3.12.0

# Code formatting & linting
black==24.1.1
isort==5.13.2
flake8==7.0.0
mypy==1.8.0

# Type hints
types-requests
types-beautifulsoup4

# SEO checks — new tools (Phase 7)
textstat==0.7.3
python-whois==0.9.4
dnspython==2.7.0
tldextract==5.1.3
cachetools==5.5.2
waybackpy==3.0.6

# Playwright stealth (reduces CAPTCHA blocking)
playwright-stealth==1.0.6

# Dev / CI tooling (not needed in production)
pytest==8.3.5
pytest-cov==5.0.0
ruff==0.9.10
mypy==1.15.0
